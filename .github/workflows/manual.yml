name: Clojurescript Track Test

on: [push, pull_request]

defaults:
  env:


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 12
      - name: where is node now?
        run: node -v
